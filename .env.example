##############################
# 🔐 AUTH CONFIG
##############################
# Used for JWT signing — must be kept secret in production
# ⚠️ For local testing only (ChangeMe)
JWT_SECRET=supersecretkey

##############################
# 🗃️ DATABASE CONFIG
##############################
# SQLite DB path for logging usage
DB_PATH=data/usage.db

##############################
# 🧠 LLM MODE SELECTION
##############################
# Available options:
# - simulation  → mock echo mode
# - openai      → OpenAI via API key (not included here)
# - ollama      → Local model via Ollama
LLM_MODE=simulation

# Default local model used when LLM_MODE=ollama
OLLAMA_MODEL=llama3

##############################
# 📊 OBSERVABILITY PORTS
##############################
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
API_PORT=8000

##############################
# 📈 GRAFANA ACCESS
##############################
# ⚠️ Used for initial dashboard provisioning and local testing only (ChangeMe)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=llmops
GRAFANA_ALLOW_ANON=true
